#include <iostream>
#include <vector>

using namespace std;

void draw(vector<bool>, vector<bool>, vector<bool>);

int main(){
	int n;
	cout << "n = ";
	cin >> n;
}

void help(){
}

void draw(vector<int> column, vector<int>){
//column[i] means queen at the ith row is at column[i]th column
}
